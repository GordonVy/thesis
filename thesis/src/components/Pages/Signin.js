import React from "react";
import { Link } from 'react-router-dom';

const Signin = () => {
    return (
      <div>
        <div className="sign-in">Hello World</div>
        <Link to="/">Homepage</Link>
      </div>
    );
}

export default Signin;
